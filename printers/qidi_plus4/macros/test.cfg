[gcode_macro test]
gcode:

  {% set count = namespace(value=0) %}
  {% for i in (1,5) %}
    {% set count.value = count.value + 1 %}
    M117 first { count.value }
  {% endfor %}

  M117 second { count.value }

